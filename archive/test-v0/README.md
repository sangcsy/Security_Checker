# Archive Test v0

This folder keeps the old 20-item test implementation for reference only.

Rules:
- The active release line is `v1.0` under the root `scripts/` and `config/` folders.
- The active `v1.0` content is the promoted 64-item implementation.
- Files in this archive are not deployment targets.

Version mapping:
- Former development version `2.1.0` with 64 items -> current release `v1.0`
- Former 20-item test implementation -> `archive/test-v0`
